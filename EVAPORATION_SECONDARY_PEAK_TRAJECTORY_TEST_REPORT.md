# 蒸发时间次峰轨迹诊断：修复、复现与验证报告

日期：2026-07-22

本地分支：`codex/evap-secondary-trajectory-diagnostics-test`
受众：轨迹模拟代码维护者与数值验证人员

> **后续控制更新：** 本报告记录的是首轮 20×20 网格诊断。后续三 seed 的 0/20/1000/2000 网格、4/30-rank、27 条失败重放及 B 点 Rmax=2/3/5 控制已经完成；C 点原 KS 偏移由 20×20 粗网格解释。最新结论和最终干净提交见 `EVAPORATION_SECONDARY_PEAK_CONTROL_FOLLOWUP_REPORT.md`，其结论取代本文中关于“C 点偏移尚未解释”和“下一步”的陈述。

## 技术摘要

本轮已修复审阅指出的四类诊断缺陷，并完成 30 条完成事件、17 条删失事件、诊断开关对账、B 点 2,017 条趋势样本及 A/B/C 各约 10,000 条正式样本。

- **诊断实现已通过。** 逃逸状态精确落在 `Rmax=2 Rsun`；Kepler 快进和普通积分跨太阳表面的驻留时间均完整计入；事件在传播循环中实时记录；trace 选择改用固定 `trace_seed`；summary 保存初始条件和两份完整 `std::mt19937` 状态。最终 CTest 17/17 通过。
- **基础 summary、事件轨迹与驻留时间契约现均通过。** 上一版报告中的“输出契约通过”应改为“基础 summary 契约通过，事件轨迹与驻留时间契约尚未完成”；本版 v2 修复后，全部 metadata 不变量在所有正式输出中均为 `true`。
- **B 点次峰趋势和日志开销已确认。** 2,017 条完成事件按旧寿命口径得到峰位 2.560/4.240、权重 0.644/0.356、相对 10,284 条基线 KS `D=0.0162`。固定 8,000 输入轨迹的单进程诊断开/关基准耗时 3.751/3.800 秒，未检测到可分辨开销；旧寿命文件字节一致。
- **正式复现不是三点全通过。** B 点全部统计门槛通过；A 点长峰相对 10k 基线偏移 0.155 dex，超过 0.15 dex 门槛 0.005 dex，但相对 100k 大样本只偏 0.048 dex；C 点峰位和权重通过，但 KS `D=0.0583>0.03`，寿命分布整体约短移 0.1 dex。
- **当前物理分类仍为“证据不足”。** 双峰本身稳定复现，B 点长寿命支与更高太阳外驻留占比显著相关；但 C 点整体分布偏移和约 `8e-4–1e-3` 的数值失败率尚未解释，不能据此完成“动力学分群”归因。

总体评估：**代码诊断可共享；物理复现结论需附带保留，进入边界/容差控制实验前应先处理 C 点偏移和能量漂移失败。**

## 正式样本显示稳定双峰，但 C 点整体分布未复现

峰位均为 `log10(lifetime_final_unbinding_s / s)`，模式分数按“短寿命；长寿命”排列。峰检测和 KS 使用原报告的同一分析函数。

| 点 | 完成事件 | 新峰位 | 基线峰位 | 新权重 | 基线权重 | KS D | 判定 |
|---|---:|---|---|---|---|---:|---|
| A：`sigma=1e-32 cm2` | 11,869 | 2.480; 4.651 | 2.478; 4.496 | 0.615; 0.385 | 0.616; 0.384 | 0.0152 | KS/权重通过；长峰对 10k 基线边界性超差 0.005 dex |
| B：`sigma=1e-34 cm2` | 10,058 | 2.520; 4.076 | 2.533; 4.067 | 0.656; 0.344 | 0.636; 0.364 | 0.0116 | 全部通过 |
| C：`sigma=1e-36 cm2` | 9,975 | 2.165; 4.076 | 2.154; 4.167 | 0.159; 0.841 | 0.129; 0.871 | 0.0583 | 峰位/权重通过；KS 失败 |

验收门槛为 KS `D<0.03`、峰位差不超过 0.15 dex、模式分数差不超过 0.05。A 点相对 100,122 条大样本参考峰 4.603 的偏差为 0.048 dex，且对该大样本 KS `D=0.0119`；因此 A 更像小基线峰位估计的边界差异。C 点对 1,000,110 条大样本仍有 KS `D=0.0536`，不是只对小基线失败。

![B 点最终解绑定寿命分布](evaporation_secondary_peak_B_10000/results_-2.000000_-34.000000/lifetime_distribution_B.png)

图中标准 Scott KDE 和 0.5 倍带宽 KDE 均保留第二峰；0.05–0.2 dex 的固定分箱也均检测到两峰。需要区分两个寿命定义：旧文件对应“capture 到最终解绑定”，其双峰清晰；“capture 到 `Rmax` 验证逃逸”因增加外向传播时间，第二峰会变弱，不能混用两者做验收。

C 点的 KS 最大差异位于 `log10(t/s)≈4.11`。与基线相比，新样本从 10% 到 95% 分位大多短移约 0.10–0.18 dex；这更像整体时间尺度偏移，而不是某一个峰消失或少量异常值造成。

## 修复内容

### 逃逸边界现在精确定位

传播器不再按标量半径比例缩放三维位置。现在对 `Radius_At_Fraction` 做括区二分求根，插值得到真实三维交点，并对浮点残差做径向投影。

30 条完成事件的 `r_escape_Rsun` 全部为 `2.0000000000000004`，相对 `2 Rsun` 的最大误差为 `4.44e-16 Rsun`。正式样本的最大误差不超过 `8.88e-16 Rsun`。

### 驻留时间覆盖完整传播区间

修复包含两部分：

- bound 外部 Kepler 快进的完整 `return_time` 累计到太阳外驻留时间，并用 apoapsis 更新最大半径；
- 普通 RK 步跨越太阳表面时，对三维半径交点求根，将该步拆分为太阳内外两段。

30 条完成事件的最大闭合误差为 `4.66e-10 s`。B 点 2,017 条样本的最大闭合误差为 `8.20e-8 s`，metadata 驻留时间不变量通过。

上一版关于“长寿命支太阳外驻留更长”的数值来自不完整指标，应视为无效并删除。用修复后的 B 点 10,058 条样本重新计算，短/长支太阳外驻留占比中位数为 0.389/0.885；这是可信的描述性关联，但仍不是因果证明。

### 事件日志改为传播循环实时记录

`trajectory_events.tsv` v2 直接记录：

- `scatter_pre` / `scatter_post`；
- 每次 `candidate_unbinding` 和 `recapture`；
- `sun_exit` / `sun_entry`；
- `capture`、`escape_validated`、`censored`、`numerical_failure`。

每行包含事件发生时的 event/scatter/step index、时间、三维位置和速度、半径、径向速度、能量、角动量、target species、束缚/内外太阳状态和弹道能量漂移。

30 条完成事件产生 17,328 条事件：8,541 对 scatter pre/post、76 次候选解绑定、46 次再捕获、47 次太阳出射、17 次太阳入射、30 次捕获和 30 次验证逃逸。76/46 与 summary 逐轨迹计数完全一致。

事件顺序采用物理可实现的定义：`event_index` 严格递增；时间、scatter index 和 step index 不倒退。`scatter_pre/scatter_post` 可发生在同一积分时刻，因此不能同时要求三个物理字段逐行严格增加，否则会伪造事件时间或计数。

### trace 选择和精确重放已解耦

新增固定 `trajectory_trace_seed`。trace 选择只哈希 `(trace_seed, rank, trajectory_id)`，不再包含系统时间 `run_id`，也不消费物理 RNG。

对选中轨迹，summary 保存：

- shift 后的完整初始相空间；
- `Initial_Conditions` 前的完整 `std::mt19937` 状态；
- 正式传播前的完整 `std::mt19937` 状态。

传播器新增 PRNG 状态序列化/恢复接口，自动化测试从保存状态和初始条件重放同一轨迹，并逐事件比较类型、时间、scatter index 和 step index。

### metadata 记录实际数值设置

`run_metadata.json` schema 更新为 `trajectory-diagnostic-v2`，实际数值参数为：

| 参数 | 值 |
|---|---:|
| RK 位置容差 | 1 km |
| RK 速度容差 | 0.001 km/s |
| RK phase 容差 | `1e-7` |
| RK 绝对最大步长 | `1e6 s` |
| 最大光学深度步长 | 0.05 |
| 光学深度相对容差 | 0.01 |
| 逃逸边界 | `2 Rsun` |
| trace seed | 2026072201 |

本轮复现配置的散射率插值网格为 `20x20`；这批旧输出生成时该值只保存在配置文件中，是解释 C 点偏移时必须保留的数值设置 caveat。审阅后代码已将 `interpolation_points` 直接加入 `run_metadata.json`，后续控制实验不再依赖外部配置判断网格。

## 自动化不变量与冒烟测试全部通过

metadata 输出并强制检查：

1. summary 时间顺序和复合键唯一；
2. 完成事件逃逸半径等于 `Rmax`；
3. `time_inside + time_outside` 与 capture 后观测时间闭合；
4. event candidate/recapture 数与 summary 一致；
5. event index 严格递增，物理时间和计数器不倒退；
6. 相同 trace seed 选择相同轨迹；
7. 选中轨迹包含完整重放状态；
8. 诊断输出与旧 `evaporation_times.txt` 完成事件逐项对账。

### 30 条完成事件

单进程、固定 seed、60 条输入轨迹得到 30 条完成事件，无删失/数值失败。全部 v2 metadata 不变量均为 `true`；逃逸半径和驻留闭合结果见上文。

### 17 条删失事件

单进程、固定 seed、`maximum_number_of_scatterings=1` 得到 17 条 captured 记录：

- 17/17 为 `termination_reason=max_scatterings`；
- 17/17 `event_observed=0`；
- 17/17 的最终解绑定和逃逸字段为 `NaN`；
- 实时事件类型包含 capture、scatter pre/post 和 censored；
- 全部 v2 metadata 不变量均为 `true`。

### 诊断开关字节对账和开销

固定输入轨迹数、关闭墙钟截断后，诊断开/关的 `evaporation_times.txt` 字节一致。墙钟截断不用于字节对账，因为它本身依赖系统负载。

在 B 点固定 8,000 条输入轨迹、单进程、2% trace 的基准中：

| 指标 | 诊断开 | 诊断关 |
|---|---:|---:|
| 完成事件 | 2,182 | 2,182 |
| 总散射数 | 106,141 | 106,141 |
| 模拟时间 | 3.751 s | 3.800 s |
| 进程实耗时 | 3.91 s | 3.93 s |
| 输出目录大小 | 3.2 MB | 328 KB |

单次运行的时间差为 −1.3%，方向与“日志更慢”相反，故只能判定为未检测到超过约 2% 噪声的开销；不能把它解释为性能提升。日志的主要成本是磁盘体积，约增加 10 倍。

### 构建与回归测试

| 测试 | 结果 |
|---|---:|
| Release 构建 | 通过 |
| `git diff --check` | 通过 |
| 完整 CTest（含 2/3/4-rank MPI） | 17/17 通过 |
| 精确边界单元测试 | 通过 |
| trace seed 稳定选择测试 | 通过 |
| RNG 状态精确重放测试 | 通过 |
| 诊断输出契约与不变量测试 | 通过 |

## 限制、风险与稳健性判断

### 数值失败率仍高于项目警戒线

| 点 | 模拟轨迹 | 数值失败 | 失败率 | captured 中无效记录 |
|---|---:|---:|---:|---:|
| A | 26,464 | 2 | `7.6e-5` | 6（4 max-scatter + 2 energy-drift） |
| B | 36,935 | 36 | `9.75e-4` | 4 energy-drift |
| C | 220,751 | 180 | `8.15e-4` | 27 energy-drift |

B/C 失败率高于当前 `1e-4` 警戒线。失败记录未写入完成寿命，因而不直接污染峰检测；但如果失败概率与寿命分支相关，仍可能产生选择偏差。正式物理归因前必须做失败轨迹的重放和容差敏感性测试。

### 正式复现没有完全满足三点联合验收

A 点只有一个 0.005 dex 的边界性峰位超差，且相对更大样本通过；B 点完整通过；C 点则是明确的 KS 失败。因而本轮不能宣称“三点正式复现全部通过”。

### 当前证据支持但不证明外部轨道分支

B 点短/长支的太阳外驻留占比中位数为 0.389/0.885，capture 半径中位数为 0.472/0.603 `Rsun`，capture 能量中位数为 −9.21/−7.23 eV。长支更浅地被俘获、更多时间处于太阳外，符合长周期外部 bound Kepler 弧段的解释。

再捕获并不是 B 点的强分离变量：短/长支至少一次 recapture 的比例为 0.281/0.302。因此目前更像“外部 bound 轨道时间尺度”而不是“仅由 recapture 次数决定”，但仍需事件级条件分布和控制实验确认。

## 建议的下一步

1. 优先重放 B/C 的 `energy_drift_escape` 轨迹，比较失败前后的步长、能量漂移和边界状态。
2. 在 C 点做 `20x20` 与生产插值网格对照，并做 RK 容差收紧 10 倍、最大步长减半；重新检查约 0.1 dex 的整体短移和 KS。
3. 在 B 点执行 `Rmax=2/3/5 Rsun` 控制，分别比较最终解绑定寿命和验证逃逸寿命，判断边界只改变定义尾段还是也改变状态机。
4. 用已保存的 RNG 状态对短峰、谷区和长峰各重放 100–300 条，核对外部 Kepler 弧段、再捕获和能量漂移的条件分布。
5. 只有 C 点 KS 和 B/C 数值失败风险得到解释后，再给出最终原因分类。

## 可复核输出

- 完成事件冒烟：`evaporation_diagnostic_completed_smoke_v2_single/results_-2.000000_-32.000000/`
- 删失事件冒烟：`evaporation_diagnostic_censored_smoke_v2_single/results_-2.000000_-32.000000/`
- B 点 2,017 条趋势样本：`evaporation_secondary_peak_B_2000/results_-2.000000_-34.000000/`
- 诊断开销基准：`evaporation_secondary_peak_B_benchmark_on/` 与 `evaporation_secondary_peak_B_benchmark_off/`
- A/B/C 正式样本：`evaporation_secondary_peak_A_10000/`、`evaporation_secondary_peak_B_10000/`、`evaporation_secondary_peak_C_10000/`
- 复现配置：`tests/config_evaporation_secondary_peak_A_10000.cfg`、`tests/config_evaporation_secondary_peak_B_10000.cfg`、`tests/config_evaporation_secondary_peak_C_10000.cfg`

本轮最终分类：**双峰稳定存在；B 点数据支持外部 bound 轨道时间尺度的描述性解释；C 点整体分布偏移和能量漂移失败尚未排除，故物理成因仍为“证据不足”。**
